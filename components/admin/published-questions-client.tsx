'use client'
export function PublishedQuestionsClient({
  initialQuestions,
  initialTotal,
}: {
  initialQuestions: unknown[]
  initialTotal: number
}) {
  return <div>Published Questions ({initialTotal} total) — coming soon</div>
}
